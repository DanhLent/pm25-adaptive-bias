# Historical IP Design Notes

The numbered files in this directory preserve the project's design evolution.
They are not the current canonical interface specification. For the engineering
freeze architecture and APB3 interface, use `PM25_SOC_IP_FINALIZATION_SPEC.md`
and `docs/ip/`.
